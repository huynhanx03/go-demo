# Ports

Port Interfaces. Defines the contracts (interfaces) that the Core layer expects the Adapters to implement. This allows the Core to remain independent of specific technologies (Dependency Inversion).
