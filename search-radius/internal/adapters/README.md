# Adapters

Interface Adapters. This layer holds the implementations that convert data from the format most convenient for the use cases and entities to the format most convenient for external agencies (Database, Web, UI).
