# Core

The heart of the application. It contains the high-level policy, business rules, and domain logic. This layer MUST NOT depend on any external libraries, frameworks, or drivers (databases, HTTP, etc.).
