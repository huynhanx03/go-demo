# DTO

Data Transfer Objects (DTOs). These define the structure of data moving between the outer layers (adapters) and the core layer. They help decouple the domain entities from external API contracts.
